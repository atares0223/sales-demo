spring:
  config:
    import:
      - nacos:share
      - nacos:user
      - nacos:share-sentinel
      - nacos:share-seata
  cloud:
    nacos:
      discovery:
        group: sales
        namespace: prod
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        group: sales
        file-extension: yaml
        namespace: prod
        username: nacos
        password: nacos
    openfeign:
      http2client:
        enabled: true