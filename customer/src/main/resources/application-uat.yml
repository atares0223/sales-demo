spring:
  config:
    import:
      - nacos:share
      - nacos:customer
  cloud:
    nacos:
      discovery:
        group: sales
        namespace: uat
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        group: sales
        file-extension: yaml
        namespace: uat
        username: nacos
        password: nacos


