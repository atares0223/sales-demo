spring:
  config:
    import:
      - nacos:share
      - nacos:customer
      - nacos:share-sentinel
      - nacos:share-seata
  cloud:
    nacos:
      discovery:
        group: sales
        namespace: dev
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        group: sales
        file-extension: yaml
        namespace: dev
        username: nacos
        password: nacos


  rabbitmq:
    address-shuffle-mode: none
    host: localhost
    port: 5672
    username: pivot_v2
    password: Pivot143!
    virtual-host: /
    # openfeign:
    #   http2client:
    #     enabled: true



